/**
 * Conceptual questions ONLY — never code-retrieval. Code lives on screen, not
 * in the transcript, so "show me the debounce implementation" fails by
 * construction. The first impression must be the system working. Every
 * question here is answered by episodes present in the cached corpus.
 */
export const SUGGESTED_QUESTIONS: readonly string[] = [
  'What is an execution context and how is it created?',
  'What is hoisting in JavaScript?',
  'What is the difference between undefined and not defined?',
  'Why are promises better than callbacks?',
  'How does async/await actually work?',
  'How does the this keyword behave in different contexts?',
];
